export enum DealApprovalStatus {
  pending = 1,
  active = 2,
  declined = 3,
}

export enum DealPaymentTerms {
  letterOfCredit = 1,
  invoiceFactoring = 2,
  discounting = 3,
  cashAgainstTheDocument = 4,
}
